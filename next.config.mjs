/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["images.unsplash.com", "cdn.dummyjson.com", "fakestoreapi.com"],
  },
};

export default nextConfig;
